import java.util.Scanner;

public class MainProcess {
    public static void main(String[] args) {
        Macro.printMacro();
        /*
        Scanner scanner = new Scanner(System.in);

        String termString = scanner.nextLine();
        Term newTerm = new Term(termString);
        System.out.println(newTerm.check());

        String expressionString = scanner.nextLine();
        expressionString = expressionString.replaceAll("\\s*", "");
        Expression expression = new Expression(expressionString);
        System.out.println(expression.check());
        */
    }
}
//-3*sin(x)**13 + cos(x)**10*x**20*x*13*sin(x)